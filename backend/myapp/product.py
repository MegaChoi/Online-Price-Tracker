class Product:
    def __init__(self, url, title, image, currentPrice, originalPrice, category):
        self.url = url
        self.title = title
        self.image = image
        self.currentPrice = currentPrice
        self.originalPrice = originalPrice
        self.category = category